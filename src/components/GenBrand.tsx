/**
 * Highlights the syllable "gen" with brand styling.
 * Usage: <GenBrand>gen</GenBrand> renders the styled span.
 */
const GenBrand = () => (
  <span className="text-primary font-semibold italic">gen</span>
);

export default GenBrand;
